<?xml version="1.0" encoding="UTF-8"?>
<templates id="template" xml:space="preserve">

    <t t-name="pos_concept_invoice.OrderReceipt" t-inherit="point_of_sale.OrderReceipt" t-inherit-mode="extension">
        <xpath expr="//OrderDisplay" position="replace">
            <div t-if="order.concept_invoice_name" class="orderline pt-2 pb-2">
                <div class="d-flex justify-content-between">
                    <span class="text-wrap d-inline fw-bolder">
                        <t t-esc="order.concept_invoice_name"/>
                    </span>
                    <span class="pos-receipt-right-align font-monospace">
                        <t t-esc="formatCurrency(order.priceIncl)"/>
                    </span>
                </div>
                <div class="pos-receipt-left-padding text-muted" style="font-size: 0.85em;">
                    1.00 x <t t-esc="formatCurrency(order.priceIncl)"/>
                </div>
            </div>
            <OrderDisplay t-else="" order="order" t-slot-scope="scope" mode="'receipt'">
                <t t-set="line" t-value="scope.line"/>
                <Orderline line="line" showTaxGroup="showTaxGroupLabels" mode="'receipt'" basic_receipt="props.basic_receipt">
                    <li t-if="line.customerNote" class="customer-note w-100 p-2 my-1 rounded text-break">
                        <i class="fa fa-sticky-note me-1" role="img" aria-label="Customer Note" title="Customer Note"/>
                        <t t-esc="line.customerNote" />
                    </li>
                </Orderline>
            </OrderDisplay>
        </xpath>
    </t>

</templates>
