<?xml version="1.0" encoding="UTF-8"?>
<templates id="template" xml:space="preserve">

    <t t-name="pos_centralized_payment.ProductScreen"
       t-inherit="point_of_sale.ProductScreen"
       t-inherit-mode="extension">

        <!-- Desktop: gate the ActionpadWidget Pay button on canPay -->
        <xpath expr="//ActionpadWidget" position="attributes">
            <attribute name="showActionButton">canPay and !currentOrder?.isEmpty()</attribute>
        </xpath>

        <!-- Desktop: show "Enviar a Caja" button for non-managers, in the same spot as Pay -->
        <xpath expr="//ActionpadWidget" position="after">
            <div t-if="!canPay and !currentOrder?.isEmpty()"
                 class="mw-100 container validation p-0">
                <div class="d-flex gap-2">
                    <button class="pay-queue-button button btn btn-warning btn-lg py-3 d-flex align-items-center justify-content-center flex-fill"
                            t-on-click="() => this.clickQueueOrder()">
                        <i class="fa fa-paper-plane me-2"/>
                        Enviar a Caja
                    </button>
                </div>
            </div>
        </xpath>

        <!-- Mobile (switchpane): hide Pay button for non-managers -->
        <xpath expr="//button[hasclass('pay-button')]" position="attributes">
            <attribute name="t-if">!pos.scanning and canPay</attribute>
        </xpath>

        <!-- Mobile (switchpane): show "Enviar a Caja" button for non-managers -->
        <xpath expr="//button[hasclass('pay-button')]" position="after">
            <button t-if="!pos.scanning and !canPay and !currentOrder.isEmpty()"
                    class="btn-switchpane btn btn-warning btn-lg flex-grow-1"
                    t-on-click="() => this.clickQueueOrder()">
                <span class="d-block">Enviar a Caja</span>
                <span t-esc="total"/>
            </button>
        </xpath>

    </t>

</templates>
